#include<cstdio>
#include<vector>
#include<algorithm>
const int N = 10000;
int t, n, m;
char a[N], b[N];
struct {
	char c;
	int x, y;
} jizz;
int main(){
	scanf("%d", &t);
	for(; t; --t){
		scanf("%d%d%s%s", &n, &m, a, b);
	}
}