#include<iostream>
using namespace std;

#define OW0 ios_base::sync_with_stdio(0);cin.tie(0);cout.tie(0);
constexpr int MAXN = 1e5;

int c[MAXN], h[MAXN];

int main(){OW0
	int n, t;
	cin >> n >> t;
	for(int i = 0; i < n; i++){
		cin >> c[i] >> h[i];
	}
	
	return 0;
}