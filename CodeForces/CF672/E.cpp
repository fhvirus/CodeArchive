#include<iostream>
#include<algorithm>
#include<vector>
using namespace std;

#define justinlaiorz ios_base::sync_with_stdio(0);cin.tie(0);cout.tie(0);
int n, a[80];
int main(){
	justinlaiorz
	cin >> n;
	for(int i = 0; i < n; ++i) cin >> a[i];
	return 0;
}